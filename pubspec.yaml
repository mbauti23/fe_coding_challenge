name: fe_coding_challenge
description: A presentation of Front End capacities.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  authentication_repository:
    path: packages/authentication_repository
  bloc: ^7.2.1
  cloud_firestore: ^2.5.3
  cupertino_icons: ^1.0.2
  equatable: ^2.0.3
  firebase: ^9.0.2
  firebase_auth: ^3.1.3
  firebase_core: ^1.7.0
  flow_builder: ^0.0.4
  flutter:
    sdk: flutter
  flutter_bloc: ^7.3.1
  google_fonts: ^2.0.0
  uuid: ^3.0.5
  responsive_framework: ^0.1.4
  
dev_dependencies:
  flutter_lints: ^1.0.0
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
